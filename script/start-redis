#!/bin/bash
set -euo pipefail

if [[ $(docker inspect -f {{.ID}} ttt_redis 2> /dev/null) != "" ]]; then
  docker rm -f ttt_redis
fi

docker run \
--detach \
--name ttt_redis \
--volumes-from ttt_redis_data \
redis
